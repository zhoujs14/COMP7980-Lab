<html>

<head>
  <title>Search Result</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
  <style>
    #container {
      flex-wrap: wrap;
      justify-content: center;
    }

    .card {
      width: 15rem;
    }

    .card-img-top {
      width: 14rem;
      height: 20rem;
      object-fit: cover
    }
  </style>
</head>

<body>
  <h1 style="text-align: center">Search Result</h1>
  <div class="row" id="container">
    <% for (var booking of bookings) { %>
      <div class="card" onclick="onClick('<%= booking._id %>')">
        <img class="card-img-top" src="./img/<%= booking.superhero %>.jpg" alt="<%= booking.superhero %>">
        <div class="card-body">
          <p>email: <%= booking.email %>
          </p>
          <p>numTickets: <%= booking.numTickets %>
          </p>
          <p>payment method: <%= booking.payment %>
          </p>
        </div>
      </div>
      <% } %>
  </div>

  <hr>
  <% for (var i=1; i <=pages; i++) { %>
    [<a href="/searchBookings?perPage=6&page=<%= i %><%= parameters %>">
      <%= i %>
    </a>]

    <% } %>
      <script>
        function onClick(id) {
          window.location.href = "http://localhost:3000/bookings/update/" + id
        }
      </script>
</body>

</html>